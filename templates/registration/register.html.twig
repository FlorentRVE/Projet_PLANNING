
{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<div class="bg-sky-200 min-h-screen">
    <h1 class="text-center text-3xl uppercase text-slate-900 font-bold py-5 shadow-xl">👤 Nouvel utilisateur</h1>

    <div class="bg-gray-800 p-7 mx-auto my-6 rounded-xl w-4/5 shadow-xl">


        <div class="w-11/12 mx-auto flex flex-col">
            
            {{ form_start(registrationForm) }}
                <div class="mb-8 flex flex-col">
                    {{ form_label(registrationForm.username, 'Identifiant', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                    {{ form_widget(registrationForm.username, {'attr': {'class': 'p-2 rounded-lg'}}) }}

                    <div class="text-red-500 font-semibold mb-2">
                        {{ form_errors(registrationForm.username) }}
                    </div>
                </div>

                <div class="mb-8 flex flex-col">
                    {{ form_label(registrationForm.plainPassword, 'Mot de passe', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                    {{ form_widget(registrationForm.plainPassword, {'attr': {'class': 'p-2 rounded-lg'}}) }}

                    <div class="text-red-500 font-semibold mb-2">
                        {{ form_errors(registrationForm.plainPassword) }}
                    </div>
                </div>

                <div class="mb-8 flex flex-col">
                    {{ form_label(registrationForm.matricule, 'Matricule', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                    {{ form_widget(registrationForm.matricule, {'attr': {'class': 'p-2 rounded-lg flex gap-2 p-2'}}) }}

                    <div class="text-red-500 font-semibold mb-2">
                        {{ form_errors(registrationForm.matricule) }}
                    </div>
                </div>
                
                <div class="mb-8 flex flex-col">
                    {{ form_label(registrationForm.categorie, 'Catégorie', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                    {{ form_widget(registrationForm.categorie, {'attr': {'class': 'p-2 rounded-lg flex gap-2'}}) }}
                </div>
                
                <div class="mb-8 flex flex-col">
                    {{ form_label(registrationForm.roles, 'Roles', {'label_attr': {'class': 'text-slate-100 uppercase font-semibold mb-2'}}) }}
                    {{ form_widget(registrationForm.roles, {'attr': {'class': 'text-slate-100 flex gap-2'}}) }}
                </div>


                {{ form_errors(registrationForm, {'class': 'text-red-600 uppercase font-semibold mb-2'}) }}

                <div class="flex justify-center md:justify-between items-center">
                    <div></div>
                    <button type="submit" class="bg-green-500 rounded-2xl text-center hover:brightness-125 shadow-lg font-bold px-7 py-3 self-end w-full md:w-1/3 text-lg">Créer l'utilisateur</button>
                </div>

            {{ form_end(registrationForm) }}
        </div>
    </div>

    <div class="flex justify-between w-4/5 mx-auto">
        <a href="{{ path('app_user_index') }}"  class="bg-sky-800 text-slate-50 rounded-2xl text-center px-5 py-3 hover:brightness-125 shadow-lg font-semibold">Retour à la liste</a>
    </div>
    </div>
{% endblock %}