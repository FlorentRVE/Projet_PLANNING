<form method="post" action="{{ path('app_roulement_delete', {'id': roulement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ roulement.id) }}">
    <button class="btn">Delete</button>
</form>
